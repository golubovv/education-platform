Для начала работы необходимо проделать несколько шагов

-------------------------------     Шаг 1     -------------------------------------------------------------
Список необходимых пакетов
asgiref==3.7.2              //// Пакет для ассинхронной работы Джанго
Django==3.2                 //// Собсссно  сам Джанго
phonenumbers==8.13.24       //// Пакет для работы с номерами телефонов, является производным от libphonenumber (Google)
Pillow==10.1.0              //// Пакет для работы с изображениями
psycopg2==2.9.9             //// Пакет используется для подключения Джанго к Posgresql
python-dotenv==1.0.0        //// Пакет для работы с .env файлом
pytz==2023.3.post1          //// библиотека для работы со временными зонами на Python
sqlparse==0.4.4             //// Пакет Python, который использует регулярные выражения для анализа SQL
tzdata==2023.3              //// Пакет предназначен в качестве запасного варианта для систем, в которых не установлены данные о системном часовом поясе

Для установки всех пакетов можно использовать команду
                    pip install -r requirements.txt
Для установки отдельных пакетов используйте команду
                    pip install asgiref==3.7.2

+ установленный на системе postgresql v. xxx

*для справки* в проекте на 11.2023 используются шаблоны. В проекте используется bootstrap v4.6, подключен статически, лежит в папке static
-------------------------------    Конец Шаг 1     ----------------------------------------------------------


-------------------------------    Шаг 2    ------------------------------------------------------------------
В директории education_platform/edu создать файл .env, в этом файле указать приватные переменные
ниже находится пример заполненого файла:

        ***********************    Файл .env   ******************************
SECRET_KEY = %cie*mik8m%*_z=#r-%$)9ji#=$ztfft06jk@j4f74i50*z)cf         //// Секретный ключ ......
DEBUG = True                                                            //// Режим отладки Да/Нет

EMAIL_PASSWORD =
EMAIL_ADDRESS =

ENGINE = django.db.backends.postgresql
DATABASE = MyNameDataBase       //// Название базы данных
USER = NameUser                 //// Имя пользователя для авторизации в БД
PASSWORD = UserPassword         //// Пароль для пользователя в БД
DB_HOST = localhost             //// .... Хост по умолчанию localhost
DB_PORT = 5432                  //// .... Порт по умолчанию 8000, но если не пойдет что-то, то можно удалить

        ***********************  Конец файл .env ******************************

-------------------------------    Конец Шаг 2     -----------------------------------------------------------

На данном этапе созданны модели, проверенна возможность внесение данных через админку.